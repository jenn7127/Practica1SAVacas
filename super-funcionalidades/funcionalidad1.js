/**
 * Super funcionalidad 1 - indica si un valor es positivo o no
 * @param value
 * @returns {boolean}
 */
function getIsPositive(value){
    // Suponer que esta funcion hace muchas cosas complicadas, etc
    // 1
    return Boolean(value >= 0);
}


module.exports = getIsPositive;